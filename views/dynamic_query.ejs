<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Show Data</title>
    <style>
        .components{
            display:flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            row-gap: 10px;
        }
        body{
            overflow: hidden;
        }
    </style>
</head>

<body>
    <div class="components">
        <%-include('./partials/query_input.ejs')%>
        <% if (result.length != 0) { %>
            <%-include('./partials/query_output.ejs',{result,keys , query2})%>
            <%-include('./partials/dynamic_query_component.ejs',{p , maxlength})%>
        <% } %>
    </div>
</body>

</html>